<%= form_for(@thermostat_model) do |f| %>
  <% if @thermostat_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thermostat_model.errors.count, "error") %> prohibited this thermostat_model from being saved:</h2>

      <ul>
      <% @thermostat_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :Thermostat_id %><br>
    <%= f.text_field :Thermostat_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

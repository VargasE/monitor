<%= form_for(@observacion) do |f| %>
  <% if @observacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@observacion.errors.count, "error") %> prohibited this observacion from being saved:</h2>

      <ul>
      <% @observacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :issue_id %><br>
    <%= f.text_field :issue_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@thermosta_model) do |f| %>
  <% if @thermosta_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thermosta_model.errors.count, "error") %> prohibited this thermosta_model from being saved:</h2>

      <ul>
      <% @thermosta_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :thermostat_id %><br>
    <%= f.text_field :thermostat_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
